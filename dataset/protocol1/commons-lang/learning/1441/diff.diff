281,282c281
<         while (throwable != null && !list.contains(throwable)) {
<             list.add(throwable);
---
>         while (throwable != null && !list.contains(throwable)) {            list.add(throwable);
