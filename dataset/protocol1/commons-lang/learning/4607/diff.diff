680c680,681
<             if (hexDigits > 8 || hexDigits == 8 && firstSigDigit > '7') { // too many for an int
---
>             if (hexDigits > 8 || hexDigits == 8 && firstSigDigit > 
> '7') { // too many for an int
