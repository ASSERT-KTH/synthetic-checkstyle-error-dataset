46c46,47
<         final int milliseconds = (minutes + (hours * MINUTES_PER_HOUR)) * MILLISECONDS_PER_MINUTE;
---
>         final int milliseconds = 
> (minutes + (hours * MINUTES_PER_HOUR)) * MILLISECONDS_PER_MINUTE;
