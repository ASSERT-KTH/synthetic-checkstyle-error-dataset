30,31c30
<  * This class is a UI thread for drawing the {@link BallItem}, and provide the method for suspend
<  * and resume. It hold the reference of {@link BallItem} to delegate the draw task.
---
>  * This class is a UI thread for drawing the {@link BallItem}, and provide the method for suspend * and resume. It hold the reference of {@link BallItem} to delegate the draw task.
