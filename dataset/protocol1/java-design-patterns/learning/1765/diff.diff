100c100
<       LotteryTicketCheckResult checkResult = service.checkTicketForPrize(id, winningNumbers);
---
>       LotteryTicketCheckResult checkResult 	= service.checkTicketForPrize(id, winningNumbers);
