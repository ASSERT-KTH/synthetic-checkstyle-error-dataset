121,122c121
<    * Writes the outgoing {@link DatagramPacket} to the channel. The intended receiver of the
<    * datagram packet must be set in the <code>data</code> using
---
>    * Writes the outgoing {@link DatagramPacket} to the channel. The intended receiver of the   * datagram packet must be set in the <code>data</code> using
