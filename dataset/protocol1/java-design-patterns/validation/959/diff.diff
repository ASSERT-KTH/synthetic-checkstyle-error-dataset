30,31c30
<  * means value objects' equality are not based on identity. Two value objects are equal when they
<  * have the same value, not necessarily being the same object..
---
>  * means value objects' equality are not based on identity. Two value objects are equal when they * have the same value, not necessarily being the same object..
