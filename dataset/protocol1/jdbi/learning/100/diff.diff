58,59c58
<             Class<?> elementType = erasedType.getComponentType();
<             return lookup.apply(elementType)
---
>             Class<?> elementType = erasedType.getComponentType();            return lookup.apply(elementType)
