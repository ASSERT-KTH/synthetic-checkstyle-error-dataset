51c51,52
<         return containerType instanceof Class ? Optional.of(ELEMENT_TYPES.get(containerType)) : Optional.empty();
---
>         return containerType instanceof Class 
> ? Optional.of(ELEMENT_TYPES.get(containerType)) : Optional.empty();
