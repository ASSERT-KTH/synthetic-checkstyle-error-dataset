38,39c38
<                 TransactionIsolationLevel currentLevel = h.getTransactionIsolationLevel();
<                 if (currentLevel != isolation && isolation != TransactionIsolationLevel.UNKNOWN) {
---
>                 TransactionIsolationLevel currentLevel = h.getTransactionIsolationLevel();                if (currentLevel != isolation && isolation != TransactionIsolationLevel.UNKNOWN) {
