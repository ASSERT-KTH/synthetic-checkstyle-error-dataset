87,88c87
<         for (int i = 0; i < canonicalNames.length; i++) {
<             result[i] = parseCanonicalName(canonicalNames[i]).getSecond();
---
>         for (int i = 0; i < canonicalNames.length; i++) {            result[i] = parseCanonicalName(canonicalNames[i]).getSecond();
