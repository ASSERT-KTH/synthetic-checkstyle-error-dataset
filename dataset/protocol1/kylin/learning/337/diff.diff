116c116
<         return retryCount != 0 && (System.currentTimeMillis() - firstSleepTime >= timeoutMs);
---
>         return retryCount != 0 && (System.currentTimeMillis() - firstSleepTime >= timeoutMs)	;
