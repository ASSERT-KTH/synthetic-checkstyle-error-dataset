48c48,49
<             final Pair<Integer, String> result = context.getConfig().getCliCommandExecutor().execute(getCmd(), patternedLogger);
---
>             final Pair<Integer, String> result = context.getConfig().getCliCommandExecutor().execute
> (getCmd(), patternedLogger);
