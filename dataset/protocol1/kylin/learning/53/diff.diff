88,89c88
<                     cols[i] = String.valueOf(DateFormat.stringToMillis(cols[i]));
<             }
---
>                     cols[i] = String.valueOf(DateFormat.stringToMillis(cols[i]));            }
