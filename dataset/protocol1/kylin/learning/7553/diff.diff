177,178c177
<                     int childIdx = columns.trueBitIndexOf(childEntry.getKey());
<                     int parentIdx = columns.trueBitIndexOf(parent);
---
>                     int childIdx = columns.trueBitIndexOf(childEntry.getKey());                    int parentIdx = columns.trueBitIndexOf(parent);
