71,72c71
<             this.childOffsetMask = ~(((long) (BIT_IS_LAST_CHILD | BIT_IS_END_OF_VALUE)) << ((sizeChildOffset - 1) * 8));
<             this.firstByteOffset = sizeChildOffset + 1; // the offset from begin of node to its first value byte
---
>             this.childOffsetMask = ~(((long) (BIT_IS_LAST_CHILD | BIT_IS_END_OF_VALUE)) << ((sizeChildOffset - 1) * 8));            this.firstByteOffset = sizeChildOffset + 1; // the offset from begin of node to its first value byte
