94,95c94
<         if (encodingName.startsWith(FixedLenDimEnc.ENCODING_NAME) && (type.isIntegerFamily() || type.isNumberFamily())) {
<             logger.warn(colRef + " type is " + type + " and cannot apply fixed_length encoding");
---
>         if (encodingName.startsWith(FixedLenDimEnc.ENCODING_NAME) && (type.isIntegerFamily() || type.isNumberFamily())) {            logger.warn(colRef + " type is " + type + " and cannot apply fixed_length encoding");
