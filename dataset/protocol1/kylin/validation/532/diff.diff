85,86c85
<             if (type.isDate()) {
<                 encoding = encodingName = DateDimEnc.ENCODING_NAME;
---
>             if (type.isDate()) {                encoding = encodingName = DateDimEnc.ENCODING_NAME;
