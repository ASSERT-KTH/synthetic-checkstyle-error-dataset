175c175,176
<                 tableService.calculateCardinality(table.trim().toUpperCase(Locale.ROOT), submitter, project);
---
>                 tableService.calculateCardinality(table.trim
> ().toUpperCase(Locale.ROOT), submitter, project);
