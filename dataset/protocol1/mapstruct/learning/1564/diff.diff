24,25c24
<  * <strong>Note:</strong> the usage of this annotation is <em>optional</em> when used in the {@link Mapper#uses()}
<  * if no source parameters are part of the signature, i.e. it is declared without parameters or only with
---
>  * <strong>Note:</strong> the usage of this annotation is <em>optional</em> when used in the {@link Mapper#uses()} * if no source parameters are part of the signature, i.e. it is declared without parameters or only with
