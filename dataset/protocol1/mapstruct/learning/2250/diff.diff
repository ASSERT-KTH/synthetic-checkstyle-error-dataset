10,11c10
<  * is not always correct. For example when compiling with JDK 9 and source version 8 classes from different modules
<  * are available by {@link javax.lang.model.util.Elements#getTypeElement(CharSequence)} but they are actually not
---
>  * is not always correct. For example when compiling with JDK 9 and source version 8 classes from different modules * are available by {@link javax.lang.model.util.Elements#getTypeElement(CharSequence)} but they are actually not
