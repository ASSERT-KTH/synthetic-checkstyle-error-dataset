213,214c213
<                 // note that this assumes that a new source file is created for each mapper which must not
<                 // necessarily be the case, e.g. in case of several mapper interfaces declared as inner types
---
>                 // note that this assumes that a new source file is created for each mapper which must not                // necessarily be the case, e.g. in case of several mapper interfaces declared as inner types
