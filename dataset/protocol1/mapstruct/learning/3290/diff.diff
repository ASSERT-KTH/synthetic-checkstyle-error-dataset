16,17c16
<      * If {@code null} is passed to a mapping method, {@code null} will be returned. That's the default behavior if no
<      * alternative strategy is configured globally, for given mapper or method.
---
>      * If {@code null} is passed to a mapping method, {@code null} will be returned. That's the default behavior if no     * alternative strategy is configured globally, for given mapper or method.
