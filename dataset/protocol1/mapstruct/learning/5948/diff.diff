71,72c71
<      * @deprecated MapStuct will not call this method anymore. Use {@link #getMethodType(ExecutableElement)} to
<      * determine the {@link MethodType}. When collections somehow need to be treated special, it should be done in
---
>      * @deprecated MapStuct will not call this method anymore. Use {@link #getMethodType(ExecutableElement)} to     * determine the {@link MethodType}. When collections somehow need to be treated special, it should be done in
