87,88c87
<             progressExecutionMonitor.check( execution( part * (i + 1), config ) );
<             assertTrue( progressExecutionMonitor.getProgress() < total );
---
>             progressExecutionMonitor.check( execution( part * (i + 1), config ) );            assertTrue( progressExecutionMonitor.getProgress() < total );
