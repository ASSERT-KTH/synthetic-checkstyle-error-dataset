330,331c330
<         // Looks like there's no mapping, so we'd like to do a page fault.
<         LatchMap.Latch latch = pagedFile.pageFaultLatches.takeOrAwaitLatch( filePageId );
---
>         // Looks like there's no mapping, so we'd like to do a page fault.        LatchMap.Latch latch = pagedFile.pageFaultLatches.takeOrAwaitLatch( filePageId );
