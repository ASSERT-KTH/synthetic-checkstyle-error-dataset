29c29
<                 expectedVersion, foundVersion, possibleToAutomaticallyUpgrade ? "can" : "cannot") + msg );
---
>                 expectedVersion, foundVersion, possibleToAutomaticallyUpgrade ? "can" 	: "cannot") + msg );
