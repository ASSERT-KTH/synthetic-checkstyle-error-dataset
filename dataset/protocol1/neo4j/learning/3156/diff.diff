92c92,93
<                     l = labels = Values.stringArray( ls.toArray( new String[ls.size()] ) );
---
>                     l = labels = Values.stringArray( 
> ls.toArray( new String[ls.size()] ) );
