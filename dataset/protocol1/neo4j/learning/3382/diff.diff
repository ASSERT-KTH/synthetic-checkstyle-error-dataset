108c108,109
<                     Processor[] newProcessors = Arrays.copyOf( processors, requestedNumber );
---
>                     
> Processor[] newProcessors = Arrays.copyOf( processors, requestedNumber );
