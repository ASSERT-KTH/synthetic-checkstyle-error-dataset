123,124c123
<         execute( "lockFileAndFailToDeleteDirectory", failedTestListener );
<         File lockedFile = CONTEXT.getValue( LOCKED_TEST_FILE_KEY );
---
>         execute( "lockFileAndFailToDeleteDirectory", failedTestListener );        File lockedFile = CONTEXT.getValue( LOCKED_TEST_FILE_KEY );
