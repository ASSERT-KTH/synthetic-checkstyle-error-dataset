96,97c96
<                 writerConfigFactory = () -> IndexWriterConfigs.population( descriptor.analyzer() );
<             }
---
>                 writerConfigFactory = () -> IndexWriterConfigs.population( descriptor.analyzer() );            }
