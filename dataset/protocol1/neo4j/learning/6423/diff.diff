177c177
<         try ( PageCursor cursor = file.io( startOffset / file.pageSize(), PF_SHARED_READ_LOCK ) )
---
>         try ( PageCursor cursor = file.io( 	startOffset / file.pageSize(), PF_SHARED_READ_LOCK ) )
