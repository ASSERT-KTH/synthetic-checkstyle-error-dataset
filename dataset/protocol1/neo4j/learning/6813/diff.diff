112,113c112
<         when( pageFile.io( anyLong(), anyInt() ) ).thenReturn( pageCursor );
<         when( pageCache.map( eq( storeFile ), anyInt() ) ).thenReturn( pageFile );
---
>         when( pageFile.io( anyLong(), anyInt() ) ).thenReturn( pageCursor );        when( pageCache.map( eq( storeFile ), anyInt() ) ).thenReturn( pageFile );
