101c101,102
<         EnumMap<IndexSlot,IndexPopulator> populators = providers.map( provider -> provider.getPopulator( descriptor, samplingConfig ) );
---
>         EnumMap<IndexSlot,IndexPopulator> populators = providers.map( 
> provider -> provider.getPopulator( descriptor, samplingConfig ) );
