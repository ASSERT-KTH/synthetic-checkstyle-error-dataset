209c209
<         try ( Transaction tx = database.getGraph().beginTransaction( implicit, AUTH_DISABLED ) )
---
>         try ( Transaction tx = database.getGraph().beginTransaction( implicit, 	AUTH_DISABLED ) )
