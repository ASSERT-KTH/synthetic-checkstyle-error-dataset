60,61c60
<      * and return the "raw" array for that given index so that it can be used directly in multiple calls,
<      * knowing that the returned array holds the given index.
---
>      * and return the "raw" array for that given index so that it can be used directly in multiple calls,     * knowing that the returned array holds the given index.
