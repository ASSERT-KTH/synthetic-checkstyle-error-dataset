111c111,112
<     default <T> long updateWithArray( long intermediateHash, T[] array, ToLongFunction<T> projectionToLong )
---
>     default <T> long updateWithArray( long 
> intermediateHash, T[] array, ToLongFunction<T> projectionToLong )
