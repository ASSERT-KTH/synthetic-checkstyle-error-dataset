197c197
<                         } while (i < getSources().length && (patchFile == null || !patchFile.exists()));
---
>                        } while (i < getSources().length && (patchFile == null || !patchFile.exists()));
