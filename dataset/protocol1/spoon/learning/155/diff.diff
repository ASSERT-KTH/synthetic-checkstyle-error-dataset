334c334
< 		while (Modifier.isStatic(clazz.getModifiers()) == false && (clazz = clazz.getEnclosingClass()) != null) {
---
> 		while (Modifier.isStatic(clazz.getModifiers() ) == false && (clazz = clazz.getEnclosingClass()) != null) {
