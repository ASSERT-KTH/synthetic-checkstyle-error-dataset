60,61c60
< 	public boolean replaceNode(RootNode oldNode, RootNode newNode) {
< 		for (int i = 0; i < nodes.size(); i++) {
---
> 	public boolean replaceNode(RootNode oldNode, RootNode newNode) {		for (int i = 0; i < nodes.size(); i++) {
