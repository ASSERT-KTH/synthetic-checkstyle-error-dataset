80,81c80
<     // An eval function can only return a SINGLE value (i.e. String)
<     // So in this simple brute force implementation we provide the input and the requested output field.
---
>     // An eval function can only return a SINGLE value (i.e. String)    // So in this simple brute force implementation we provide the input and the requested output field.
