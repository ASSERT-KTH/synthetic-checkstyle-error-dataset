58c58,59
<         walkList = new WalkList(requiredPattern, matcher.getLookups(), matcher.getLookupSets(), verbose);
---
>         walkList = new WalkList(requiredPattern
> , matcher.getLookups(), matcher.getLookupSets(), verbose);
