68,69c68
<             "The value should be a string but it is a " + valueNode.getNodeId().name());
<         return ((ScalarNode)valueNode).getValue();
---
>             "The value should be a string but it is a " + valueNode.getNodeId().name());        return ((ScalarNode)valueNode).getValue();
